alloy:
  configMap:
    create: true
    content: |
      logging {
        level  = "info"
        format = "logfmt"
      }

      discovery.kubernetes "pods" {
        role = "pod"
      }

      discovery.kubernetes "nodes" {
        role = "node"
      }

      discovery.kubernetes "services" {
        role = "service"
      }

      // Discover pods in k8sgpt-operator-system namespace
      discovery.kubernetes "k8sgpt_pods" {
        role = "pod"
        namespaces {
          names = ["k8sgpt-operator-system"]
        }
      }

      // Collect logs from k8sgpt-operator pods
      loki.source.kubernetes "k8sgpt_results" {
        targets    = discovery.kubernetes.k8sgpt_pods.targets
        forward_to = [loki.process.k8sgpt_results.receiver]
      }

      // Forward k8sgpt operator pod logs to Loki as-is (for debugging)
      // Structured Result CRD data is pushed separately by k8sgpt-result-scraper CronJob
      loki.process "k8sgpt_results" {
        forward_to = [loki.write.default.receiver]

        stage.static_labels {
          values = {
            source = "k8sgpt",
          }
        }
      }

      // Write to Loki
      loki.write "default" {
        endpoint {
          url = "http://loki.loki.svc:3100/loki/api/v1/push"
        }

        external_labels = {
          cluster = "bookish-octo-robot",
          source  = "k8sgpt",
        }
      }

  extraEnv:
    - name: CLUSTER_NAME
      value: bookish-octo-robot

serviceAccount:
  create: true

rbac:
  create: true
