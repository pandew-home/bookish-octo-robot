apiVersion: v1
kind: Secret
metadata:
  name: k8sgpt-ai-secret
  namespace: k8sgpt-operator-system
type: Opaque
stringData:
  # Managed by CI via kubectl create secret --dry-run | kubectl apply.
  # Do NOT commit real values here — set OPENROUTER_API_KEY in GitHub Actions secrets.
  openai-api-key: "REPLACE_AT_DEPLOY_TIME"
