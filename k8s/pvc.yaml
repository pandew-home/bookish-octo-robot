apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devops-chatbot-pvc
  namespace: devops-chatbot
  labels:
    app: devops-chatbot
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
