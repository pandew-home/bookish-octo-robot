# Dashboard JSON is checked in verbatim. The kube-prometheus-stack Grafana
# sidecar discovers ConfigMaps with label grafana_dashboard=1 in any
# watch-namespace and loads them directly — no templating required.
{}
