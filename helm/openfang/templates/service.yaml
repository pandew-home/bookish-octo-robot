apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfang.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openfang.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "openfang.selectorLabels" . | nindent 4 }}
